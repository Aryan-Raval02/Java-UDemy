~> Relational Operators :
    When we use relational oprators then it returns boolean value.
    <
    <=
    >
    >=
    ==
    !=


~> Logical Operators : Use for Combining two or more relational statements
                        It works only on boolean values.
        &&
        ||
        !

~> Conditional Statements :

if
if else
if elseif ... else
nested if

~> switch case :
    It use for menu driven program.
    Switch is fasater than if statements.
    alternate of if elseif ... else
    more readable

    switch(expression)
    {
        case 1: //body one
                break; //brsk is important in switch
        case 2: //body two  
                break;
        .
        .
        .
        case n: //body n
                break;
        default://body default  
                break;
    }

    expression allowance : byte, short, int, char, String
                                             'a'   "Sun"